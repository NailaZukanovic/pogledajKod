const stateReducer = (state, action) => {
    switch (action.type) {
        case "updateProps":
            return Object.assign(Object.assign({}, state), action.payload);
        case "set":
            const { name, value } = action.payload;
            return Object.assign(Object.assign({}, state), { [name]: value });
        case "triggerDialog":
            const selected = action.payload.selected;
            return Object.assign(Object.assign({}, state), { dialog: action.payload.status, selectedRange: (selected === null || selected === void 0 ? void 0 : selected.event_id) ? null : selected, selectedEvent: (selected === null || selected === void 0 ? void 0 : selected.event_id) ? selected : null });
        case "triggerLoading":
            return Object.assign(Object.assign({}, state), { loading: action.payload });
        default:
            return state;
    }
};
export { stateReducer };
