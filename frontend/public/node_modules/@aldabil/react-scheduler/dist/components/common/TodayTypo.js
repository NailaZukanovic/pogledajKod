import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Typography } from "@mui/material";
import { format, isToday } from "date-fns";
const TodayTypo = ({ date, onClick }) => {
    return (_jsxs("div", { children: [_jsx(Typography, Object.assign({ style: {
                    fontWeight: isToday(date) ? "bold" : "inherit",
                }, color: isToday(date) ? "primary" : "inherit", className: onClick ? "rs__hover__op" : "", onClick: (e) => {
                    e.stopPropagation();
                    if (onClick)
                        onClick(date);
                } }, { children: format(date, "dd") }), void 0),
            _jsx(Typography, Object.assign({ color: isToday(date) ? "primary" : "inherit", style: {
                    fontWeight: isToday(date) ? "bold" : "inherit",
                    fontSize: 11,
                } }, { children: format(date, "eee") }), void 0)] }, void 0));
};
export default TodayTypo;
