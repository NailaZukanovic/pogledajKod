import { jsx as _jsx } from "react/jsx-runtime";
import { useAppState } from "../../hooks/useAppState";
import NavigateBeforeRoundedIcon from "@mui/icons-material/NavigateBeforeRounded";
import NavigateNextRoundedIcon from "@mui/icons-material/NavigateNextRounded";
import { IconButton } from "@mui/material";
const LocaleArrow = ({ type, onClick }) => {
    const { direction } = useAppState();
    let Arrow = NavigateNextRoundedIcon;
    if (type === "prev") {
        Arrow =
            direction === "rtl" ? NavigateNextRoundedIcon : NavigateBeforeRoundedIcon;
    }
    else if (type === "next") {
        Arrow =
            direction === "rtl" ? NavigateBeforeRoundedIcon : NavigateNextRoundedIcon;
    }
    return (_jsx(IconButton, Object.assign({ style: { padding: 2 }, onClick: onClick, onDragOver: (e) => {
            e.preventDefault();
            if (onClick) {
                onClick();
            }
        } }, { children: _jsx(Arrow, {}, void 0) }), void 0));
};
export { LocaleArrow };
