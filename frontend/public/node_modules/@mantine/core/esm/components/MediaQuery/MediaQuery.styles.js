import { createStyles } from '@mantine/styles';

var useStyles = createStyles((theme, { smallerThan, largerThan, query, styles }) => {
  const media = {};
  if (smallerThan) {
    media[`@media (min-width: ${theme.fn.size({ size: smallerThan, sizes: theme.breakpoints }) + 1}px)`] = styles;
  }
  if (largerThan) {
    media[`@media (max-width: ${theme.fn.size({ size: largerThan, sizes: theme.breakpoints })}px)`] = styles;
  }
  if (query) {
    media[`@media ${query}`] = styles;
  }
  return { media };
});

export default useStyles;
//# sourceMappingURL=MediaQuery.styles.js.map
